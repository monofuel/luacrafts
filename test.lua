local Lang = require('lang')

-- possible features:
--  detect cycles?
--  flatten requires?
--  linting
--  formatting
--  minify / obfuscate?

function main()
    lang = Lang:new('lang.lua')
end

main()
